export interface PhilipsHueSseResponse {
    id?: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    id_v1: string;
    on?: {
        on: boolean;
    };
    status?: string;
}
